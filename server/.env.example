# Server configuration
PORT=4000
NODE_ENV=development

# MongoDB connection (opt-in)
# Set USE_DB=true to enable database connection; otherwise server runs without DB
USE_DB=false
MONGO_URI=mongodb://localhost:27017/sauti
DB_NAME=sauti

# Feature flags
FEATURE_THREADS=true
