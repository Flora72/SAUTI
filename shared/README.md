# Shared

Place shared constants, validation schemas, and types/interfaces used by both client and server here.

Examples:
- constants/privacyLevels.js
- validation/journalEntry.schema.json
- types/index.d.ts (if using TypeScript)
